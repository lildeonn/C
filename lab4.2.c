#include <stdio.h>
#include "lab4.h"

int per(){
	double P;
	
	P = q + w + e;
	
	return 0;
}

int plo(){
	double S;
	
	S = sqrt(P/2 * (P/2 - q) * (P/2 - w) * (P/2 - e));
	
	return 0;
}
